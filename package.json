{
  "name": "stackwise",
  "version": "1.0.0",
  "private": true,
  "description": "Stackwise — Demand forecasting for small e-commerce",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^14.14.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
